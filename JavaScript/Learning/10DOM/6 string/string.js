// parseInt()
// String.fromCharCode(65+ )
// !isNaN()

// backslash escape character.


text.charAt(0);
charCodeAt(0);
at(2);
// name[2]
slice(start, end)
substring(start, end)
substr(start, length)

toUpperCase();
toLowerCase();
concat(); t1.concat(" ",t2);
trim();
trimStart();
trimEnd();
text.padEnd(4,"0");
repeat(4);
replace('m','d');
text.replaceAll();
indexOf();
lastIndexOf();